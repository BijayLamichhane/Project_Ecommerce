import React, { useState, useEffect, useRef } from "react";
import { useSearchParams, Link } from "react-router-dom";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { api } from "../lib/axios";
import { Conversation, Message } from "../types";
import { useAuth } from "../hooks/useAuth";
import { useSocket } from "../hooks/useSocket";
import { formatDate } from "../lib/utils";
import { MessageSquare, Send, User } from "lucide-react";

export function MessagesPage() {
  const [searchParams] = useSearchParams(); const initialRecipient = searchParams.get("recipient");
  const { user } = useAuth(); const socket = useSocket(); const queryClient = useQueryClient();
  const [activeConversationId, setActiveConversationId] = useState<string | null>(null); const [messageContent, setMessageContent] = useState(""); const messagesEndRef = useRef<HTMLDivElement>(null);
  const { data: conversations, isLoading: loadingConversations } = useQuery({ queryKey: ["conversations"], queryFn: async () => { const { data } = await api.get("/messages/conversations"); return data.data as Conversation[]; } });
  const { data: activeThread } = useQuery({ queryKey: ["conversation-messages", activeConversationId], queryFn: async () => { if (!activeConversationId) return null; const { data } = await api.get(`/messages/conversations/${activeConversationId}`); return data.data; }, enabled: !!activeConversationId });
  useEffect(() => { if (!activeConversationId && conversations?.length) setActiveConversationId(conversations[0]?.id || conversations[0]?._id || null); }, [conversations, activeConversationId]);
  useEffect(() => { if (!socket || !activeConversationId) return; socket.emit("join_conversation", activeConversationId); const handleNewMessage = (newMsg: Message) => queryClient.setQueryData(["conversation-messages", activeConversationId], (old: any) => old ? { ...old, messages: [...(old.messages || []), newMsg] } : old); socket.on("new_message", handleNewMessage); return () => { socket.emit("leave_conversation", activeConversationId); socket.off("new_message", handleNewMessage); }; }, [socket, activeConversationId, queryClient]);
  useEffect(() => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" }); }, [activeThread?.messages]);
  const sendMessageMutation = useMutation({ mutationFn: async () => { if (!messageContent.trim()) return; const activeConv = conversations?.find((c) => (c.id || c._id) === activeConversationId); const currentUserId = user?.id || user?._id; const recipientId = String(activeConv?.customerId) === String(currentUserId) ? activeConv?.sellerId : activeConv?.customerId || initialRecipient; if (!recipientId) return; const { data } = await api.post("/messages/send", { conversationId: activeConversationId, recipientId, content: messageContent.trim() }); return data.data; }, onSuccess: () => { setMessageContent(""); queryClient.invalidateQueries({ queryKey: ["conversations"] }); queryClient.invalidateQueries({ queryKey: ["conversation-messages", activeConversationId] }); } });
  const handleSend = (e: React.FormEvent) => { e.preventDefault(); sendMessageMutation.mutate(); };
  const messages: Message[] = activeThread?.messages || []; const currentConv = conversations?.find((c) => (c.id || c._id) === activeConversationId); const currentUserId = user?.id || user?._id; const otherParty = String(currentConv?.customerId) === String(currentUserId) ? currentConv?.seller : currentConv?.customer;
  return <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"><div className="bg-white rounded-3xl border border-slate-200 shadow-sm overflow-hidden grid grid-cols-1 md:grid-cols-12 h-[750px]"><div className="md:col-span-4 border-r border-slate-200 flex flex-col"><div className="p-4 border-b border-slate-100"><h2 className="text-base font-bold text-slate-900">Messages</h2></div><div className="flex-1 overflow-y-auto divide-y divide-slate-100">{loadingConversations ? <div className="p-4 space-y-3">{Array.from({ length: 4 }).map((_, i) => <div key={i} className="h-16 bg-slate-100 rounded-xl animate-pulse" />)}</div> : !conversations?.length ? <div className="p-8 text-center text-xs text-slate-400"><MessageSquare className="w-8 h-8 mx-auto text-slate-300" /><p>No conversations yet</p></div> : conversations.map((conv) => { const convId = conv.id || conv._id; const partner = String(conv.customerId) === String(currentUserId) ? conv.seller : conv.customer; return <button key={convId} onClick={() => setActiveConversationId(convId)} className={`w-full text-left p-4 hover:bg-slate-50 transition flex items-start gap-3 ${convId === activeConversationId ? "bg-indigo-50/70 border-l-4 border-indigo-600" : ""}`}><div className="w-10 h-10 rounded-full bg-slate-200 overflow-hidden flex-shrink-0">{partner?.avatarUrl ? <img src={partner.avatarUrl} alt={partner.name} className="w-full h-full object-cover" /> : <User className="w-5 h-5 m-2.5 text-slate-500" />}</div><div className="flex-1 min-w-0"><h4 className="text-xs font-bold text-slate-900 truncate">{partner?.name || "Lender / Renter"}</h4>{conv.product && <div className="text-[11px] font-medium text-indigo-600 truncate">{conv.product.name}</div>}</div></button>; })}</div></div><div className="md:col-span-8 flex flex-col h-full bg-slate-50/50">{activeConversationId ? <><div className="p-4 bg-white border-b border-slate-200 flex items-center gap-3"><div className="w-9 h-9 rounded-full bg-slate-200 overflow-hidden">{otherParty?.avatarUrl ? <img src={otherParty.avatarUrl} alt={otherParty.name} className="w-full h-full object-cover" /> : <User className="w-5 h-5 m-2 text-slate-500" />}</div><div><h3 className="text-xs font-bold text-slate-900">{otherParty?.name}</h3>{currentConv?.product && <Link to={`/products/${currentConv.product.id || currentConv.product._id}`} className="text-[11px] text-indigo-600 hover:underline">Listing: {currentConv.product.name}</Link>}</div></div><div className="flex-1 overflow-y-auto p-4 space-y-3">{messages.map((msg) => { const msgId = msg.id || msg._id; const isMine = String(msg.senderId) === String(currentUserId); return <div key={msgId} className={`flex flex-col ${isMine ? "items-end" : "items-start"}`}><div className={`max-w-md px-4 py-2.5 rounded-2xl text-xs leading-relaxed shadow-sm ${isMine ? "bg-indigo-600 text-white rounded-br-none" : "bg-white text-slate-900 border border-slate-200 rounded-bl-none"}`}>{msg.content}</div><span className="text-[10px] text-slate-400 mt-1 px-1">{formatDate(msg.createdAt, "h:mm a")}</span></div>; })}<div ref={messagesEndRef} /></div><form onSubmit={handleSend} className="p-4 bg-white border-t border-slate-200 flex gap-2"><input type="text" placeholder="Type your message..." value={messageContent} onChange={(e) => setMessageContent(e.target.value)} className="flex-1 px-4 py-2.5 text-xs bg-slate-100 border border-transparent rounded-xl outline-none focus:bg-white focus:border-indigo-500" /><button type="submit" disabled={!messageContent.trim() || sendMessageMutation.isPending} className="px-4 py-2.5 rounded-xl bg-indigo-600 hover:bg-indigo-700 disabled:opacity-40 text-white transition flex items-center justify-center"><Send className="w-4 h-4" /></button></form></> : <div className="flex-1 flex flex-col items-center justify-center text-center p-8 text-slate-400 space-y-2"><MessageSquare className="w-12 h-12 text-slate-300" /><h4 className="text-sm font-bold text-slate-700">Select a conversation</h4><p className="text-xs">Chat directly with equipment lenders about availability and pickup details</p></div>}</div></div></div>;
}
